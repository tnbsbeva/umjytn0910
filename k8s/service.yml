apiVersion: v1
kind: Service
metadata:
  name: ikjhdtrnjr
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ikjhdtrnjr"
    port: 8080
  selector:
    app: ikjhdtrnjr
